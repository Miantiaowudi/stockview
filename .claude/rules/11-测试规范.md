---
alwaysApply: false
description: 项目的测试规范，包括测试覆盖要求与质量门禁。当需要确认测试要求时读取此规则。
---

# 测试规范

## 测试要求

- **类型检查**：所有代码必须通过 TypeScript 类型检查
- **代码质量**：关键业务逻辑应包含必要的测试用例
- **ESLint**：代码必须通过 ESLint 检查

## 禁止事项

- ❌ 硬编码 API Key
- ❌ `as any` 类型断言
- ❌ 提交 `.env.local` 到 Git
