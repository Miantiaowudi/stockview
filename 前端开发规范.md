# 前端开发规范

## 1. 技术栈要求

| 类别 | 推荐技术 |
|------|----------|
| 框架 | React 18+ / Next.js 14+ |
| 语言 | TypeScript 5+ |
| 样式 | Tailwind CSS / CSS Modules |
| 状态管理 | Zustand / Jotai（简单）/ React Query（服务端状态）|
| 构建工具 | Vite / Next.js 内置 |
| 代码规范 | ESLint + Prettier |
| 测试 | Vitest + React Testing Library |
| UI 组件 | 按需选择（shadcn/ui、Ant Design 等）|

---

## 2. 项目目录结构

```
src/
├── app/                    # 路由/页面组件（Next.js App Router）
│   ├── layout.tsx
│   ├── page.tsx
│   └── api/               # API 路由
├── components/            # 共享组件
│   ├── ui/               # 基础 UI 组件（Button, Input 等）
│   ├── layout/           # 布局组件（Header, Sidebar 等）
│   └── features/         # 业务功能组件
├── hooks/                # 自定义 React Hooks
├── lib/                  # 工具函数/第三方库封装
├── stores/               # 状态管理
├── types/                # TypeScript 类型定义
├── styles/               # 全局样式
└── utils/                # 纯工具函数
```

**规则：**
- 按功能（feature）组织代码，而非按类型
- 组件相关文件（`.tsx`, `.test.tsx`, `.module.css`）就近存放

---

## 3. 编码规范

### 3.1 命名约定

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件 | PascalCase | `UserProfile.tsx` |
| 文件 | camelCase / PascalCase | `useAuth.ts`, `Button.tsx` |
| 函数 | camelCase | `getUserData()` |
| 常量 | UPPER_SNAKE_CASE | `API_BASE_URL` |
| 类型/接口 | PascalCase | `UserData`, `ApiResponse` |
| 枚举 | PascalCase + UPPER_SNAKE | `Status.ACTIVE` |

### 3.2 TypeScript 规范

```typescript
// ✅ 推荐：显式返回类型（公共 API）
export function calculateStockPrice(data: StockData): number {
  return data.price * data.quantity;
}

// ✅ 推荐：使用接口描述对象结构
interface StockChartProps {
  symbol: string;
  period: '1D' | '1W' | '1M' | '1Y';
  onDataLoad?: (data: ChartData[]) => void;
}

// ❌ 禁止：使用 any
function processData(data: any) { }

// ❌ 禁止：不必要的类型断言
const value = data as any as string;
```

### 3.3 组件规范

```typescript
// ✅ 推荐：函数式组件 + Props 接口
interface StockCardProps {
  symbol: string;
  price: number;
  change: number;
  onClick?: (symbol: string) => void;
}

export function StockCard({ symbol, price, change, onClick }: StockCardProps) {
  const handleClick = () => {
    onClick?.(symbol);
  };

  return (
    <div onClick={handleClick}>
      <h3>{symbol}</h3>
      <span>{price}</span>
    </div>
  );
}

// ✅ 推荐：默认导出放在文件底部
export default StockCard;
```

**组件开发原则：**
1. **单一职责**：一个组件只做一件事
2. **Props 透明**：避免深层传递，使用 Context 或状态管理
3. **受控组件优先**：表单元素推荐使用受控模式
4. **避免过度渲染**：合理使用 `useMemo`, `useCallback`

---

## 4. 样式规范

### 4.1 Tailwind CSS（推荐）

```tsx
// ✅ 推荐：使用 clsx/tailwind-merge 处理条件类名
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

// 使用示例
<div className={cn(
  "flex items-center p-4",
  isActive && "bg-blue-500",
  variant === 'large' && "text-lg"
)}>
```

### 4.2 CSS 变量（主题配置）

```css
:root {
  --color-primary: #3b82f6;
  --color-success: #22c55e;
  --color-danger: #ef4444;
  --spacing-unit: 0.25rem;
}
```

---

## 5. 状态管理规范

### 5.1 服务端状态（React Query）

```typescript
// ✅ 推荐：封装查询逻辑
export function useStockData(symbol: string) {
  return useQuery({
    queryKey: ['stock', symbol],
    queryFn: () => fetchStockData(symbol),
    staleTime: 30 * 1000, // 30秒内不重复请求
    refetchInterval: 60 * 1000, // 每分钟自动刷新
  });
}
```

### 5.2 客户端状态（Zustand）

```typescript
// ✅ 推荐：按功能拆分 store
interface WatchlistStore {
  symbols: string[];
  addSymbol: (symbol: string) => void;
  removeSymbol: (symbol: string) => void;
}

export const useWatchlistStore = create<WatchlistStore>((set) => ({
  symbols: [],
  addSymbol: (symbol) => set((state) => ({
    symbols: [...state.symbols, symbol]
  })),
  removeSymbol: (symbol) => set((state) => ({
    symbols: state.symbols.filter(s => s !== symbol)
  })),
}));
```

---

## 6. API 层规范

```typescript
// lib/api/client.ts
const apiClient = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
  timeout: 10000,
});

// 请求拦截器 - 添加认证
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// ✅ 推荐：按领域组织 API
export const stockApi = {
  getQuote: (symbol: string) => 
    apiClient.get<StockQuote>(`/stocks/${symbol}/quote`),
  
  getHistory: (symbol: string, period: string) => 
    apiClient.get<StockHistory[]>(`/stocks/${symbol}/history`, {
      params: { period }
    }),
};
```

---

## 7. 性能优化规范

### 7.1 组件优化

```typescript
// ✅ 推荐：列表使用 key，复杂组件使用 memo
const StockListItem = memo(function StockListItem({ 
  stock, 
  onSelect 
}: StockListItemProps) {
  // 组件逻辑
}, (prev, next) => prev.stock.id === next.stock.id);

// ✅ 推荐：大数据列表使用虚拟化
import { Virtualizer } from '@tanstack/react-virtual';
```

### 7.2 资源优化

- 图片使用 Next.js `Image` 组件或 WebP 格式
- 路由级代码分割（Next.js 自动处理）
- 第三方库按需导入

---

## 8. Git 工作流规范

### 8.1 分支策略

| 分支 | 用途 |
|------|------|
| `main` | 生产环境代码，保护分支 |
| `develop` | 开发分支，集成测试通过 |
| `feature/*` | 功能开发，从 develop 切出 |
| `bugfix/*` | Bug 修复 |
| `hotfix/*` | 生产环境紧急修复 |

### 8.2 提交信息格式（Conventional Commits）

```
<type>(<scope>): <description>

[optional body]

[optional footer]
```

**类型：**
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例：**
```
feat(stock-chart): 添加K线图时间周期切换功能

- 支持 1D/1W/1M/1Y 四个时间维度
- 添加缓存机制避免重复请求

Closes #123
```

---

## 9. 测试规范

### 9.1 测试文件位置

```
ComponentName.tsx
ComponentName.test.tsx        # 单元测试
ComponentName.stories.tsx     # Storybook（可选）
```

### 9.2 测试编写原则

```typescript
// ✅ 推荐：测试行为而非实现
it('should display stock price when data loaded', async () => {
  render(<StockCard symbol="AAPL" />);
  
  await waitFor(() => {
    expect(screen.getByText('$150.00')).toBeInTheDocument();
  });
});

// ❌ 禁止：测试内部状态
it('should set state correctly', () => {
  // 不要这样做
});
```

---

## 10. 代码审查（Code Review）清单

### 审查者检查项

- [ ] 代码是否符合命名规范
- [ ] 是否有 TypeScript 类型定义
- [ ] 是否有 `any` 或 `@ts-ignore`
- [ ] 组件是否过于复杂（超过 200 行需拆分）
- [ ] 是否有性能隐患（不必要的重渲染）
- [ ] 是否包含测试
- [ ] 是否处理了错误边界
- [ ] 是否遵循无障碍（a11y）标准

### 提交者自检

- [ ] 本地 lint 通过：`npm run lint`
- [ ] 类型检查通过：`npm run type-check`
- [ ] 测试通过：`npm test`
- [ ] 相关文档已更新

---

## 11. 推荐配置

### package.json 脚本

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "lint:fix": "next lint --fix",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "format": "prettier --write \"src/**/*.{ts,tsx}\""
  }
}
```

---

## 附录：规范执行工具

- **ESLint**: `eslint-config-next`, `@typescript-eslint`
- **Prettier**: 统一代码格式
- **Husky + lint-staged**: 提交前自动检查
- **GitHub Actions**: CI/CD 自动验证

---

*规范版本: 1.0*  
*最后更新: 2026年2月*
